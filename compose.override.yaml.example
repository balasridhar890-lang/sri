# Docker Compose Override Example
# Copy this file to compose.override.yaml to customize your deployment
# Docker Compose automatically merges compose.yaml with compose.override.yaml

version: '3.8'

services:
  # Backend service customizations
  backend:
    # Example: Add custom volume mounts
    # volumes:
    #   - ./custom-config:/app/config:ro
    
    # Example: Override environment variables
    # environment:
    #   LOG_LEVEL: DEBUG
    #   GUNICORN_WORKERS: 8
    
    # Example: Add custom labels for monitoring
    # labels:
    #   - "prometheus.io/scrape=true"
    #   - "prometheus.io/port=8000"
    
    # Example: Resource limits
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '2.0'
    #       memory: 2G
    #     reservations:
    #       cpus: '1.0'
    #       memory: 1G

  # Database service customizations
  db:
    # Example: Custom PostgreSQL configuration
    # command: postgres -c max_connections=200
    
    # Example: Resource limits for database
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '2.0'
    #       memory: 4G
    #     reservations:
    #       cpus: '1.0'
    #       memory: 2G

  # Example: Add nginx reverse proxy
  # nginx:
  #   image: nginx:alpine
  #   container_name: voice-assistant-nginx
  #   restart: unless-stopped
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - ./ssl:/etc/nginx/ssl:ro
  #   depends_on:
  #     - backend
  #   networks:
  #     - voice-assistant-network

  # Example: Add Redis for caching
  # redis:
  #   image: redis:7-alpine
  #   container_name: voice-assistant-redis
  #   restart: unless-stopped
  #   command: redis-server --appendonly yes
  #   volumes:
  #     - redis-data:/data
  #   networks:
  #     - voice-assistant-network
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # Example: Add monitoring with Prometheus
  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: voice-assistant-prometheus
  #   restart: unless-stopped
  #   ports:
  #     - "9090:9090"
  #   volumes:
  #     - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
  #     - prometheus-data:/prometheus
  #   networks:
  #     - voice-assistant-network

  # Example: Add Grafana for visualization
  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: voice-assistant-grafana
  #   restart: unless-stopped
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - GF_SECURITY_ADMIN_PASSWORD=admin
  #   volumes:
  #     - grafana-data:/var/lib/grafana
  #   networks:
  #     - voice-assistant-network

# Example: Add additional volumes
# volumes:
#   redis-data:
#     driver: local
#   prometheus-data:
#     driver: local
#   grafana-data:
#     driver: local
